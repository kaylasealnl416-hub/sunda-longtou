# Vercel 部署已改为 Vercel 控制台直接连接 GitHub 自动触发
# 此 Action 已停用，保留仅作记录
name: Deploy to Vercel (disabled)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Vercel deployment is handled automatically via Vercel Git integration."
